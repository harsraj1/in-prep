# The app parses Gemini and Voicebox payloads explicitly with org.json and does
# not reflect over domain models. OkHttp and Media3 publish their consumer rules,
# so no broad project keep rules are required. Keep this file intentionally
# narrow; any future reflection-based serializer must add tested model rules.
